<h2>All Orders</h2>

<% @orders.each do |order| %>
  <p>Name: <%= order.id %></p>
  <p>Line Items: </p>
  <ul>
    <% order.lineitems.each do |item| %>
    <li><%= item.quantity %> x <%= item.product.name %></li>
    <% end %>
  </ul>
<% end